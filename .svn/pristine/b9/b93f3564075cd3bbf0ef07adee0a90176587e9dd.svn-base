using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace NexGenFlow
{
    public static class TableName
    {
        private static DevExpress.Xpo.Session _session = new DevExpress.Xpo.Session();
        public static string CONS_plan_doc_keyer = (new NexGenFlow.NexGen.plan_doc_keyer(_session)).GetType().Name;
        public static string CONS_document = (new NexGenFlow.NexGen.document(_session)).GetType().Name;
        public static string CONS_package = (new NexGenFlow.NexGen.package(_session)).GetType().Name;
        public static string CONS_user = (new NexGenFlow.NexGen.user(_session)).GetType().Name;
    }
}
